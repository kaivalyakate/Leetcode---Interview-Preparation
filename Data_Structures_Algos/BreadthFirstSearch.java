import java.util.LinkedList;
import java.util.Queue;
import java.util.Scanner;

public class BreadthFirstSearch {

    // public static int ReverseNum(int num) {
    // char[] numString = ("" + num).toCharArray();
    // int i = 0, j = numString.length / 2;
    // char temp;
    // while (i < j) {
    // temp = numString[i];
    // numString[i] = numString[numString.length - 1 - i];
    // numString[numString.length - 1 - i] = temp;
    // i += 1;
    // }
    // return Integer.parseInt(new String(numString));
    // }

    public static void main(String[] args) {
        // Queue<Integer> queue = new LinkedList<>();
        // queue.add(2);
        // queue.add(4);
        // System.out.println(queue.element());

        // Scanner sc = new Scanner(System.in);
        // int num = sc.nextInt();

        // String encoding = "abcdefg";
        // encoding.indexOf("bcd");

        // System.out.println(ReverseNum(num));
        // sc.close();
    }
}
